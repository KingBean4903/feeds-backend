# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  username: String!
  displayName: String
  bio: String
  avatarUrl: String
  isFollowing: Boolean!
  followingCount: Float!
  followersCount: Float!
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Media {
  id: ID!
  ownerId: String!
  type: MediaType!
  mimeType: String!
  sizeBytes: BigInt!
  status: MediaStatus!
  durationSeconds: Int!
  thumbnailUrl: String
}

enum MediaType {
  IMAGE
  AUDIO
  VIDEO
}

"""
The `BigInt` scalar type represents non-fractional signed whole numeric values.
"""
scalar BigInt

enum MediaStatus {
  UPLOADING
  PROCESSING
  READY
  FAILED
}

type PostMedia {
  postId: ID!
  mediaId: String!
  order: Int!
  post: Post!
  media: Media!
}

type PageInfo {
  hasNextPage: Boolean!
  endCursor: String
}

type Post {
  id: ID!
  type: PostType!
  authorId: String!
  text: String
  visibility: PostVisibility!
  media: [PostMedia!]
  stats: PostStats
  author: User!
}

enum PostType {
  TEXT
  IMAGE
  AUDIO
  VIDEO
}

enum PostVisibility {
  PUBLIC
  FOLLOWERS
}

type PostStats {
  postId: ID!
  likesCount: Int!
  commentsCount: Int!
  repostsCount: Int!
  viewsCount: Int!
  post: Post!
}

type PostEdge {
  node: Post!
  cursor: String
}

type FeedConnection {
  edges: [PostEdge!]!
  pageInfo: PageInfo!
}

type Query {
  feed(userId: String!, first: Int!, after: String): FeedConnection!
  profile(id: Float!): User!
  followers(id: Float!): [User!]!
  following(id: Float!): [User!]!
}

type Mutation {
  createPost(postPayload: PostPayload!): Post!
  followUser(userId: FollowUserInput!): User!
  unfollow(userId: FollowUserInput!): User!
}

input PostPayload {
  text: String
  visibility: String
  authorId: String
  type: String
}

input FollowUserInput {
  userId: Float!
}